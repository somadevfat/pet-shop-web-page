import React from "react";

import logo from "../../../assets/images/Logo.svg";
export const Logo = () => {
  return (
    <div>
      <img src={logo} alt="logo" />
    </div>
  );
};

export default Logo;
